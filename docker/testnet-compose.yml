version: '3'
services:
  miniphp70-fpm:
    image: lejianwen/php:70fpm-mini
    #    build:
    #      context: ./miniphp70
    ports:
      - '9000:9000'
    volumes:
      - '/data/htdocs:/data/htdocs'
      - '/data/etc/php:/data/apps/php/etc'
      - '/data/logs/php:/data/logs/php'
    depends_on:
      - mariadb
    networks:
      - nmp
  #  php70-fpm:
  #    image: lejianwen/php:70fpm
  #    #    build:
  #    #      context: ./miniphp70
  #    ports:
  #      - '9001:9000'
  #    volumes:
  #      - '/data/htdocs:/data/htdocs'
  #      - '/data/etc/php:/data/apps/php/etc'
  #      - '/data/logs/php:/data/logs/php'
  #    depends_on:
  #      - mariadb
  #    networks:
  #      - nmp
  mariadb:
    image: lejianwen/mariadb:1.0
    ports:
      - '3307:3306'
    volumes:
      - '/data/mysql_data:/data/mysql_data'
      - '/data/etc/mysql/my.cnf:/etc/my.cnf'
    networks:
      - nmp
networks:
  nmp:
    driver: bridge